# 하네스 후속 개선 결과 · 2026-09-11

**H01–H04 구현·관련 로컬 검사·실모델 비교를 완료했어요.** 최종본은 지정한 네 합성 사례를 모두 완료했고, 창 전환·도우미·읽기 압축의 반복 실행이 줄었어요. 소수 사례의 결과이며 전체 창작·요약 품질 보증은 아니에요. 승인 범위는 [후속 계획](HARNESS-OPTIMIZATION-PLAN.md), 이전 결과는 [안정화 결과](STABILIZATION-RESULTS-2026-09-11.md)에 있어요.

## 비교 기준과 승인

- 기준 커밋은 `c10203ad02c279034dc2f620e11e5dad29261430`이에요. 이후 `da439a2`는 계획 문서만 추가한 커밋이에요.
- 기준 소스와 검증된 빌드를 `output/live-harness-optimization-20260911/baseline-source/`에 별도로 보관했어요. 기준 source/build ID는 `90571a626b1d154cf8c7935bc17561dbc85ff4f40500850479e1219eb606e177`, dist hash는 `2a8d82773b4231003352258b923091b9f9683a46cac25bc6ad8c3247713aa236`이에요.
- 사용자가 네 개선과 실모델 비교를 승인하고 기존 장부의 총 승인 예산을 **$150**로 높였어요. 과거 예약액 **$38.49258**을 포함하며, 일반 설정·사례·실행량은 이 총액 안에서 판단해요.
- [Google 공식 가격표](https://cloud.google.com/gemini-enterprise-agent-platform/generative-ai/pricing)를 2026-09-11 다시 확인했어요. Gemini 3.8 Flash의 global Standard 공개 단가는 100만 토큰당 입력 $0.75·출력 $3.75이며, 연말까지 50% 환급 크레딧 방식인 점과 계정 적용 여부 미확인을 유지해요. 할인 전 단가는 $1.50·$7.50이에요. 실제 실행 예약에는 기존의 더 높은 입력 $3.30·출력 $15.00 기준을 사용하고, 공급자가 제공하지 않는 실제 비용은 `null`로 남겨요.

## 구현

- **H01 도우미 압축**: 후보가 원 요청보다 작고 실제 입력 한도 안이면 채택해요. 85% 아래로 내려오거나 10% 이상 줄어야 한다는 조건은 없앴어요. 효과가 없어도 원 요청이 한도 안이면 history·도구 결과·Vertex 서명을 유지해 계속하고, 둘 다 한도를 넘으면 실패해요. 새 읽기 데이터가 없는 동안 부드러운 압축 기준으로 다시 요약하지 않으며 실제 상한 초과는 재검토해요. 완료한 변경·거절·알 수 없는 도구 교환은 정확한 인자와 결과를 별도 host reference로 보존해요. 채택 후에도 call ID 중복과 operation ID의 중복 실행을 막아요.
- **H02 의미 보존**: 자동·수동 대화 요약, 모델의 `context.write/new`, 실행 중 읽기 요약, 도우미 요약이 하나의 의미 지침을 사용해요. 화자·수신자·행동·각 약속의 독립 조건, 관찰·계획·전언·미확인 이행, 명시적 정정, 정확한 식별자와 원문 근거를 구분해요. 각 경로의 권한·저장·예약 책임은 유지해요.
- **H03 조회 후 완료**: 읽기 요약 뒤 실제로 반환된 source/hash/revision과 범위·필터·잘림 metadata를 남겨요. 검색 발췌나 부분 읽기를 전체 근거 확보로 취급하지 않고, 확보한 근거와 남은 질문을 구분하도록 안내해요. 정확한 인용·바뀐 revision·누락 범위의 재조회는 계속 허용해요.
- **H04 길이 정책**: 소비 모델의 입력 여유와 저장된 출력 상한으로 목표 요약 길이와 출력 여유를 함께 계산해요. 모델·과거 snapshot을 바꾸지 않고 해당 요약 호출에만 적용해요. 숫자 사고 budget 회귀는 현재 실제 프로토콜에서 지원하지 않는 내부 요청 방어 검사이며, 실비교의 Gemini 사고 강도는 `LOW`를 유지해요.

읽기 metadata를 정확히 남기는 만큼 입력이 늘 수 있고, 청크 경계에 가까운 사례에서는 요약 호출 수가 늘 수 있어요. 공통 지침의 추가만으로 의미 품질 개선이나 비용 감소를 확정하지 않아요.

## 로컬 검증

- 공통 정책·대화 요약·읽기 경로 focused **67 PASS**, 도우미 focused **72 PASS**예요. 신규 도우미 11개는 작은 채택·원 요청 보류·실제 Vertex encoder의 서명 유지·같은 읽기 재요약 억제·hard crossing·영수증·권한·EOF·취소·설정 변경·총 호출 한도·UTF16 조각·checkpoint CAS를 확인해요. 여기서 Vertex 응답은 합성이고 실제 공급자 호출은 아니에요.
- 최초 focused 실패는 공통 경로 10개, 도우미 신규 11개예요. 각각 허용하지 않는 모델 옵션 fixture, metadata 증가로 달라진 청크 수의 가정, 모델 갱신 payload의 비허용 fixture 필드가 원인이었어요. fixture를 고쳐 위 최종 focused 결과를 얻었으며 최초 결과를 덮지 않았어요.
- 첫 `quality:full`은 `server/model-runner.ts` 서식 1건에서 실패해 고쳤어요. 다음 실행은 품질·tooling을 통과했지만 build의 `spawn EPERM`으로 **BLOCKED**됐어요. 이전 빌드와 차단 로그를 보존하고 같은 전체 검사를 허용된 환경에서 다시 실행했어요.
- 허용된 환경의 첫 전체 검사는 **1,885 PASS / opt-in 1 SKIP**였어요. 최종 리뷰에서 읽기 요약의 고정 입력 계산이 보존할 metadata의 크기를 빠뜨린 점을 찾아 추가 보완했어요. 이제 빈 요약에 실제 metadata·안내·영수증을 모두 넣은 요청 본문을 먼저 재고 목표 길이를 정해요. 실제 읽기 6개와 쓰기 영수증을 사용하는 경계 회귀를 더한 focused는 **68 PASS**예요. 이 보완의 최초 focused는 환경 `spawn EPERM`으로 차단됐고, 허용된 환경에서 드러난 fixture의 stale prompt compilation 4건을 고친 뒤 통과했어요. 최종 소스의 전체 검사를 다시 실행해 아래 결과를 얻었어요.

첫 실비교에 사용한 소스의 Windows 게이트는 다음과 같아요. 아래 R3 후속 보완 전의 증거예요.

| 검사 | 결과 | 증거 |
| --- | --- | --- |
| `quality:full` | **1,886 PASS / opt-in 1 SKIP**; 품질·tooling·빌드 포함, 종료 코드 0 | `output/harness-optimization-quality-full-metadata-final.log` |
| `verify:smoke` | **3 PASS**, cleanup PASS | `output/playwright/2026-09-11T01-46-19-703Z-fb392057/summary.json` |
| `verify:chat-unification` | **16 PASS**, cleanup PASS | `output/playwright/chat-unification-2026-09-11T01-48-21-593Z-f35af345/summary.json` |

소스/빌드 ID는 `b9c7242b0ca622242d3858489f7428d2d8c7e9b531ade34fd86b47141aa76f8f`, dist hash는 `223ed49c3f545cb7279495b9784ab25c845037a67f8f711d86a944575d92f8d5`예요. 최소 앱 검사는 같은 소스를 다시 빌드하며, 실제 비교는 그 마지막 빌드(`2026-09-11T01:46:23.105Z`)를 고정해요. 전체 릴리스 화면 회귀는 이번 변경의 게이트가 아니므로 반복하지 않았어요.

## 실제 비교 R1·R2와 후속 수정

처음 실행기 폴더의 `--prepare`는 0회 전송으로 완료됐지만 `--preflight`에서 존재하지 않는 내부 `ProductStore.list`를 호출해 중단됐어요. 이 실패는 제품 경로가 아닌 비교 fixture 초기화 오류예요. 원래 실행기·진행 파일·template DB를 보존하고 새 평가 폴더에서 준비했어요. 이 시점의 새 모델 전송은 0회이며 기존 예약 $38.49258은 변하지 않았어요.

R2는 `c10203a` 기준과 위 `b9c7242b` 변경본에 같은 합성 입력·DB·모델 설정을 적용했어요. 각 측 38회, 총 **76회**를 전송했고 예약 **$22.376415**, 기존 장부 포함 **$60.868995 / $150**예요. 원본 증거는 `output/live-harness-optimization-20260911-r2/`에 있어요.

| 사례 | 기준 | 첫 변경본 | 해석 |
| --- | --- | --- | --- |
| 두 차례 요약 뒤 약속·정정 | 완료, 의미 PARTIAL | 완료, 의미 PARTIAL | 독립 조건·정정값·이행 미확인은 보존했으나 출처 귀속 오류가 각각 남았어요 |
| `context.write/new` 뒤 원문 회수 | 16회 소진, FAIL | 16회 소진, FAIL | 두 측 모두 write/new를 8회 반복하고 읽기·최종 답변에 도달하지 못했어요 |
| 도우미 수정·큰 자료 조회·압축 | 16회 소진, FAIL | 16회 소진, FAIL | 변경본의 완료 영수증은 후속 전송 8/8에서 정확히 보존됐지만 자료 읽기를 반복했어요 |
| 네 원문 회수 | 완료, NOT_EXERCISED | 완료, NOT_EXERCISED | 네 번 모두 4096자 부분 조회였고 읽기 압축이 발동하지 않아 H03의 실증으로 쓰지 않아요 |

반복 요약의 기준 답변은 porter의 “brass token moved”를 새벽 전 전달 주장으로 강화했고, 변경본은 사용자 메모의 cobalt 정정을 amber가 적힌 scene 4에 귀속했어요. 변경본 중간 요약에는 원문에 없는 ledger 반환을 미해결 행동으로 더한 점도 있어요. 어느 측도 전체 의미 PASS로 바꾸지 않았어요.

R2 도우미의 최초 자동 영수증 비교는 DB 원본과 UI 도구 projection을 직접 비교한 평가 오류가 있었어요. 실제 `helperOptionState`의 `program → definitions`, pending delegation 제외 규칙을 적용해 도구/native/host 기록의 정확한 일치를 다시 확인했어요. 원래 실행 결과는 수정하지 않고 `postreview-helper-receipts.json`으로 교정 근거를 남겼어요.

이 실패를 근거로 후속 제품 변경을 적용했어요. 본문은 새 창·읽기 압축 뒤 기존 메시지 순서를 유지하며 마지막에 같은 요청의 완료 기록을 전달해요. 도우미도 같은 task/segment의 완료 읽기 인자와 실제 반환 위치를 남겨요. 두 경로 모두 남은 작업을 이어가도록 안내하고 필요한 재조회는 허용해요. 요약 지침에는 전언을 강화하지 않기, 사용자 정정을 이전의 잘못된 장면에 귀속하지 않기, 미확인 결과에서 새 의무를 만들지 않기를 추가했어요.

후속 focused는 본문·요약·구성 **94 PASS**, 도우미 **74 PASS**예요. 첫 후속 focused의 중복 host 문맥 문제는 제품 수정으로 해결했고, 늘어난 완료 metadata를 수용할 입력 여유와 soft trigger 재조회 크기 가정은 fixture를 조정했어요. 환경 `spawn EPERM` 차단과 최초 실패 결과를 보존했어요.

## 최종 소스의 완료 게이트

| 검사 | 결과 | 증거 |
| --- | --- | --- |
| `quality:full` | **1,888 PASS / opt-in 1 SKIP**, 품질·tooling·빌드 포함, 종료 0 | `output/harness-optimization-r3-quality-full.log` |
| `verify:smoke` | **3 PASS**, cleanup PASS | `output/playwright/2026-09-11T02-39-34-546Z-33d0519b/summary.json` |
| `verify:chat-unification` | **16 PASS**, cleanup PASS | `output/playwright/chat-unification-2026-09-11T02-41-14-819Z-d61aef64/summary.json` |

최종 source/build ID는 `51f81e23c62e5f6c9fd4cebaad5ba63842463107cf52473087e15b651ba32843`, dist hash는 `be4df911e0c1b967e723f0d7f7ce1e609e35b3be125d8915579c3c856acfecc3`이고, 실비교에 고정한 빌드 시각은 `2026-09-11T02:39:37.781Z`예요. 위 첫 실비교의 결과와 구분하며 전체 `verify:redesign`은 반복하지 않았어요.

## 최종 실비교 R3

R3의 기준은 R2 변경본 `b9c7242b`를 소스·dist 1,273개 파일까지 봉인한 사본이고, 비교 대상은 최종 `51f81e23`이에요. 두 측의 초기 DB 바이트가 같고, Gemini `gemini-3.8-flash`, 사고 `LOW`, 출력 4,096, main/context/helper 입력 16,384/32,768/12,288을 공유해요. 각 작업의 16회 상한도 같아요. R3의 H03만 양쪽에 `offset:0, limit:8000` 전체 읽기 요청을 적용해 압축 경로를 자극했어요. 이 때문에 R2와 R3의 H03 결과를 같은 자극의 비교로 합치지 않아요.

| 사례 | R3 기준 | 최종본 | 실제 확인 |
| --- | --- | --- | --- |
| 반복 요약·약속·정정 | 3회, 완료 | 3회, 완료 | 두 측 모두 정해 둔 주체·상대·조건·별도 계획·전언·이행 미확인·정정 메모 출처를 보존했어요 |
| 모델 작성 요약·창 전환 | 16회 소진, 읽기 0, FAIL | **4회 완료** | write/new/read 각 1회, 실제 창 전환 뒤 원문 회수, `Sera → Mira`, `ORCHID-7319`, 인용 쉼표까지 일치해요 |
| 도우미 변경·읽기·압축 | 16회 소진, search/read 각 5회, FAIL | **6회 완료**, search/read 각 1회 | 옵션 변경 효과 1회, 정확한 영수증, 압축 1회 뒤 최종 답변을 확인했어요 |
| 네 원문의 읽기 압축 | 16회 소진, 읽기 30회·압축 7회, FAIL | **6회 완료**, 읽기 4회·압축 1회 | 네 전체 원문의 text/hash/range가 DB와 일치하고 최종 문장·화자·상대·코드·장면이 모두 맞아요 |

표의 읽기 사례 호출 수는 본문 Run 안의 요약 호출을 포함하고, 양쪽의 사전 source 요약 1회씩은 제외해요. 전체 합계는 기준 **52회**, 최종본 **20회**, 총 **72회**예요. 모든 실제 전송은 attempt와 1:1로 일치하고 결과가 확정됐으며, 종료 후 두 DB에 진행 중인 작업은 0개예요. 실행기는 종료 코드 0으로 닫혔어요. 기준의 세 실패가 있으므로 원본 실행 집계 `completed-with-failures-or-unexercised`는 그대로 보존해요.

도우미의 DB 전체 operation 2행은 delegation 생성 1행과 choose 1행이에요. 옵션 적용을 두 번 했다는 뜻이 아니며, 지정 operation의 choose 도구·저장 변경·pending 항목은 각각 1개예요. 창 전환 사례의 원문 읽기는 5,346자 중 4,096자인 부분 조회지만, 요청한 문장·코드·귀속은 모두 실제 반환 범위 안에 있어요. H03은 네 원문 전체를 읽은 별도 사례예요.

반복 요약의 핵심 기준은 양쪽 모두 통과했지만, 기준의 ribbon 용도를 “sealing”으로 표현한 부분과 최종본의 계획을 “internal”로 표현한 부분은 원문이 확정하지 않은 부가 추정이에요. 약속 조건·이행·출처를 바꾸지는 않았지만 원문에 없는 세부를 전혀 만들지 않는다고 주장하지 않아요. 의미 지침 각각의 독립 효과나 일반 성공률을 이 한 쌍에서 확정하지 않아요.

### 증거와 평가 후 교정

원본은 `output/live-harness-optimization-20260911-r3/`의 `PLAN.md`, `preflight.json`, `cases.json`, `progress.json`, `comparison.json`, 양쪽 SQLite와 answer/context/helper/attempt JSON이에요. 별도 검토는 다음 세 파일에 있어요.

- `semantic-review-summary-window.json`: 실제 template DB의 정정 메모와 원문·요약·최종 답변, 창 전환의 원문 범위와 인용을 대조했어요.
- `semantic-review-helper.json`·`semantic-review-helper-metadata.json`: read-only SQLite에서 도우미 변경 1회와 원형/native/host 영수증, 완료 읽기 3개의 실제 인자·반환 metadata를 대조했어요. 기준의 host 영수증 8개와 최종본의 1개가 모두 정확해요. 최초 보조 검토 parser가 native 본문의 `Request data (JSON)` 접두어를 놓쳐 host 기록을 0개로 읽은 오류는 `*-parser-first.*`로 보존하고 실제 envelope를 읽어 교정했어요. 제품이나 원본 실행 결과를 바꾼 것은 아니에요.
- `semantic-review-reads-accounting.json`: DB 원문 text/hash와 전체 반환 범위, 네 답변의 귀속, 전송·attempt·누적 비용·미완료 작업 0개를 대조했어요. 재현 스크립트는 `postreview-reads-accounting.mjs`예요.

실행기의 읽기 중복 수는 JSON 키 순서까지 비교해 기준의 중복을 10으로 계산했어요. 실제 같은 source revision/hash/range 기준으로는 네 고유 범위를 30회 읽어 **26회가 반복**이에요. 또 `stimulusStatus`의 예전 진단 문구는 네 원문을 먼저 읽고 압축 후 답한 정상 경로에도 미완료 재개가 없다고 표시해요. 실제 exercise 조건과 도구·전송 기록은 최종본의 경로를 인정하므로, 원본 파일을 바꾸지 않고 위 별도 검토에 바로잡았어요. 제품의 참조·동일 읽기 중복 제거도 현재 JSON 표현을 비교하므로 키 순서만 다른 표현까지 의미상 동일하게 정규화하지는 않아요. 정확한 참조를 보존하는 동작과 이번 완료 결과는 유지하지만 추가 중복 제거의 한계는 남아요.

### 비용과 종료

| 구간 | 새 전송 | 보수적 예약액 | 누적 예약액 |
| --- | ---: | ---: | ---: |
| 이전 안정화 장부 | 100 | $38.492580 | $38.492580 |
| R1 준비 실패 | 0 | $0 | $38.492580 |
| R2 비교 | 76 | $22.376415 | $60.868995 |
| R3 최종 비교 | 72 | $22.127190 | **$82.996185 / $150** |

이번 요청의 새 전송은 **148회**, 같은 승인 장부의 총 전송은 **248회**예요. 공급자가 실제 청구 비용을 제공하지 않아 `costUsd=null`을 유지했고, 이 표는 실제 청구액이 아닌 전송 전 보수적 예약액이에요. R3 사용량은 기준 입력 399,812·출력 12,884 토큰, 최종본 입력 143,091·출력 3,876 토큰이지만 완료 여부가 달라 일반 성능이나 가격 우위의 비율로 해석하지 않아요. 승인 범위의 구현·검증이 끝났으므로 남은 예산을 쓰기 위한 추가 실행은 하지 않았어요.
