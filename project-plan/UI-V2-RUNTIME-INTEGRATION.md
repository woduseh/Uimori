# UI v2 · 런타임 v14 통합 (2026-09-09)

원격 `main`의 UI 개편(`97efadd`)을 런타임 간소화·검사 통폐합 완료본(`5ab567a`)에 통합했어요. 헤더·한 줄 입력창·장면 탐색·실패 카드·서재/프롬프트 폴더·설정의 기본/고급 구성을 유지하고, 현재 v14 백엔드와 전역 프롬프트 계약에 맞춰 충돌을 해결했어요.

## 통합 내용

- 전역 작문·번역 작업본과 복사형 프리셋을 유지해요. 프리셋을 새로 만들거나 다른 프리셋을 열 때도 현재 작업본의 미저장 초안을 확인해요.
- 전체 원문을 평문으로 번역하고 최신 job을 편집기에 전달해요. 새 번역 확인창은 이전 성공 번역 보존과 현재 설정 재요청 계약을 설명해요.
- 장면 메뉴에 **현재 설정으로 다시 요청**을 유지해요. 취소·실패 카드를 표시하고 실행 중 카드를 봤다는 이유로 나중의 화면 밖 실패 알림을 숨기지 않아요.
- 폐기한 분할 번역·채팅별 프롬프트 기대값을 복원하지 않았어요. 기본 기능 검사와 선택적 추가 폭·성공 PNG·반복 성능 검사의 분리를 유지해요.

## 검증

- `quality:full`의 서식·lint·타입, Node 도구 **31 PASS**, 새 빌드, 단위·통합 **1,353 PASS / 1 opt-in skip**(132파일, 98.67초)을 확인했어요. 최초 샌드박스 빌드의 `spawn EPERM`은 BLOCKED로 보존하고 권한을 갖춘 재실행에서 빌드·테스트가 통과했어요.
- 앱 source/build 지문: `b26cfcad37b1cc5ebdf8fb00f0e733af6a5b493ab3e11e0019a2070414875022`.
- 첫 전체 기능 브라우저: **158 PASS / 4 FAIL**, `output/playwright/redesign-2026-09-08T15-32-06-515Z-eb262772/summary.json`. 저장 상태의 중복 locator, 새로고침 후 프롬프트 탐색 누락, 메뉴 opener 변수 누락 2건을 수정했어요. 제품 저장·편집 실패로 해석하지 않았어요.
- 수정 후 관련 기능·선택 시각 검사 **7 PASS**, `output/playwright/integration-corrections-2026-09-08T15-38-51-990Z-1993241a/summary.json`. 장면 메뉴의 여섯 폭과 설정·긴 원문 편집을 포함하며, 390px 메뉴·설정·편집기 및 1440px 채팅 PNG를 직접 확인했어요. 전체 선택 시각 suite의 실행은 아니에요.
- 후속 `npm run quality` PASS. 마지막 변경은 테스트뿐이므로 통과한 앱 빌드를 재사용해요.

최종 `npm run verify:redesign`은 **162 PASS / 0 FAIL / 0 SKIP**예요. `output/playwright/redesign-2026-09-08T15-40-30-003Z-aa3e3b0f/summary.json`에 종료 시 앱/검사 동일성·cleanup·canary 결과를 보존했어요. 앱 소스는 위 빌드와 같아요.

## 메인 반영과 운영 검증

사용자는 메인 반영·커밋·푸시·불필요한 작업트리와 브랜치 정리·Oracle 배포를 요청했고, 기존 DB 폐기와 새 DB 배포를 명시적으로 승인했어요. 배포는 새 이미지의 v14 초기화·무결성과 앱 소스 지문을 먼저 검사하고 기존 DB 파일만 교체해요. 인증 파일·접근 토큰·HTTPS 라우팅·PocketRisu는 보존해요. 배포 결과는 별도 `output/integration-ui-v2/RESULTS.md`에 보관해요.

로컬 검증은 합성 DB·provider를 사용해요. 운영 검증은 빈 DB에서 실제 Linux/Docker·신뢰된 HTTPS·인증·모바일/데스크톱 브라우저 화면을 확인하며 실제 모델 호출·창작 품질·휴대폰 실기기 검증은 포함하지 않아요. 모델과 자료는 새 DB에서 다시 등록해야 해요.
