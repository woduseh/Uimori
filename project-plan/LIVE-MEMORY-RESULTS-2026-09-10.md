# 장기 문맥 실모델 평가 · 2026-09-10

결정 2의 대체 평가예요. 사용자가 승인한 로컬 **Gemini 3.8 Flash**, 총비용 **$100 이내**, 전체 호출 횟수 제한 없음으로 수행해요. 작업별 호출·출력 한도와 취소·불확실 요청 재생 금지는 유지해요. 평가 실행과 모델 답변의 정확성, 사용자의 문학적 품질 인수를 구분해요.

## 자료와 실행 경계

- 원문: Jane Austen, [Pride and Prejudice, Project Gutenberg 1342](https://www.gutenberg.org/ebooks/1342), [사용한 영문 텍스트](https://www.gutenberg.org/cache/epub/1342/pg1342.txt).
- 61장 **162,975 o200k 토큰**. 서문·목차·라이선스는 제외하고 본문 속 삽화 캡션은 남겼어요. 반복하거나 길이를 채우는 내용을 추가하지 않았어요. Gemini의 청구 토큰과 같은 수치가 아니에요.
- 원문 파일 SHA-256: `3f6bb9d6f78e0293b56acd4714dd68cb7d6d1d293402031ce9d5a216bcaf9d75`. 각 장의 hash·범위·토큰 수는 로컬 `corpus.json`에 있어요.
- 모델은 같은 Gemini ID를 사용하는 평가용 세 프리셋이에요. 본문 입력 한도 16,384, 문맥·도우미 32,768, 출력 한도 4,096, 작업별 최대 16회로 반복 압축을 유도해요. 실제 연결은 승인한 Vertex global 연결이고 원래 사용자 DB의 설정을 바꾸지 않아요.
- 평가 채팅에 장별 원문을 authored 이력으로 추가하고 6장마다 실제 질문을 보내요. 작품에 없는 무작위 보관인·검색 구절·약속·귀환 조건을 초기 기록에 넣고, 13장에서 사용자 메모로 색상을 정정하며 27장에서 보관 위치를 바꿔요. 정답 파일은 모델에 제공하지 않아요.
- 원문을 150k 이상 직접 생성한 창작 세션이 아니에요. 알려진 소설 지식만으로 맞힐 수 있는 질문은 보조 관찰이며, 별도 무작위 사실과 출처 재조회가 문맥 보존의 주된 증거예요. 과거 사건 회수 질문과 도구 사용 지침이 있으므로 도구 사용을 완전 무유도 행동이라고 표현하지 않아요.

평가 ID는 `4e0da1f9-6fe3-4b17-af78-fac9450b52ef`, 채팅 ID는 `e119f535-a962-4029-a2bd-23dfa2ad3fb4`예요. `output/live-memory-20260910/`에 독립 SQLite, 본문 추출본, 입력·응답·도구 영수증, attempt, checkpoint, 진행·비용 및 소스/빌드 경계를 보존해요. 이 경로는 Git에 포함되지 않으며 사용자 DB나 원본 CharX를 평가 데이터로 사용하지 않았어요. 자격 파일을 복사하지 않고 토큰은 실행 프로세스 안에서만 사용해요.

## 최초 실행과 수정 경계

36장까지 질문 6개를 완료했고 본문 문맥의 자동 압축은 6회였어요. 그다음 도우미 요약 검토 작업은 `MODEL_CALL_BUDGET_EXHAUSTED`로 종료됐어요. 최종 답은 없고 `context.read` 6회·`story.search` 1회·`story.read` 0회였어요.

원인은 도우미 `context.read`가 UI 상세 응답의 과거 checkpoint 전체 본문까지 반환한 제품 문제예요. 응답은 110,081자였고 과거 checkpoint 목록만 83,605자였어요. 모델이 이 결과를 반복 조회하고 다시 압축하며 작업별 16회 예산을 소진했어요. 현재 활성 요약을 자르지 않고 활성 summary·출처·revision·사용 가능 여부·유효 메모만 전달하도록 수정하며, UI 상세 조회의 이력은 유지해요.

원래 실패 작업 `ea45a2ca-32e5-4105-bb50-2cedf73f7d54`와 전송 37회는 그대로 보존해요. 새 빌드를 명시적으로 채택한 후 새 작업 키로 도우미 검토를 재검증하고, 완료한 본문 질문은 기존 영수증을 사용해 다시 전송하지 않아요. 코드 변경 전후를 같은 빌드의 연속 검증으로 합치지 않아요.

## 중간 답변의 원문 대조

아래는 에이전트가 실제 답변을 해당 장 원문과 대조한 결과예요. 명확한 미래 사건 누출은 찾지 못했지만, 근거 귀속 오류와 과도한 단정이 있어 완전 정확으로 판정하지 않아요.

| 질문 시점 | 관찰 |
| --- | --- |
| 6장 | Elizabeth의 반감과 Darcy의 관심을 구분하고 춤 거절·눈에 대한 언급으로 뒷받침해요. |
| 12장 | 방문 목적·어머니의 계산·질병·자매의 반응은 맞아요. 다만 3마일 전체를 혼자 걸었다는 서술은 엄밀하지 않아요. Catherine과 Lydia가 Meryton까지 동행해요. |
| 18장 | Wickham의 주장과 반대 증언을 구분해요. Caroline이 선대 Darcy의 친절을 확인했다는 근거 귀속은 틀렸어요. 원문은 현 Darcy를 가리켜요. |
| 24장 | Charlotte의 생계 동기와 Elizabeth의 반감은 맞아요. Elizabeth의 주관적 신뢰 상실을 두 사람의 우정 상실로 일반화하고, Collins를 계속 회피한다는 예측을 강하게 단정해요. |
| 30장 | 과거 약혼 고백과 현재 가정 운영을 정확히 연결해요. 압축된 22장 원문을 검색·재조회하고 실제 source ID를 인용했어요. |
| 36장 | Elizabeth의 판단 변화를 구분하지만 Georgiana 유인·도주 미수라는 핵심 새 증거가 빠졌어요. Darcy의 완전한 정당성이 확인됐다는 표현은 편지 증언과 Elizabeth의 판단보다 강해요. |

30장에서는 `story.search` 4회 중 검색 결과가 0건인 경우 검색어를 고쳤고, `story.read`로 22장 source `35dbc046-38f5-4b88-a055-2f3fef3ffffd`의 9,600–10,100 문자 범위를 읽었어요. 반환된 500자와 hash가 저장한 corpus와 일치해요. 본문 요약 revision 3–6은 정정 색상의 우선성을, revision 5–6은 변경된 보관 위치도 보존했어요.

## 비용과 보존 확인

실제 `costUsd`를 공급자가 보고하지 않아 null로 유지해요. [공식 Vertex global 가격](https://cloud.google.com/gemini-enterprise-agent-platform/generative-ai/pricing)과 별도로 실행 예산 가드는 할인 없는 보수 단가(입력 $3.30 / 출력 $15.00, 각각 100만 토큰당)를 사용해요. 각 전송 전 UTF-8 길이 기반 상한과 출력 한도를 영속 기록하고 누적 예약 상한 $80에서 멈추므로 사용자 $100 한도에 여유를 둬요. 불확실한 전송의 예약 금액은 환급하지 않아요.

사용자가 확인한 실제 단가는 **입력 $0.75 / 출력 $3.75, 각각 100만 토큰당**이에요. 최초 37회는 입력 527,250·출력 35,865 토큰으로 이 단가의 추정 비용은 **$0.5299**예요. 보수 단가 계산 $2.2779와 전송 전 누적 예약 상한 $16.1093은 예산 가드 수치이며 실제 청구액이 아니에요. 응답 상태는 `completed` 26회와 정상 도구 요청인 `tool_calls` 11회예요. 도우미 작업 실패는 별도이며, 11개의 도구 요청 응답을 실패로 세지 않아요. 이 시점의 36장 원문 hash·전체 저장 원문 hash·압축 source/hash 대조에서 불일치는 없었어요.

### 도우미 재검증과 두 번째 중단

조회 축소 뒤 별도 도우미 작업은 **7회 호출, 추가 압축 0회**로 완료했어요. `context.read` 응답은 26,019자로 76.36% 줄었고, 원문 두 곳과 실제 정정 메모를 읽었어요. 두 원문 응답의 범위·hash가 원본과 일치해요. 다만 답변은 요약 범위를 전체 대화 범위로 오인하고 미래 사건을 언급했으며, 인용 범위와 증언의 확실성 표현에도 오류가 있어 의미 품질은 부분 성공이에요.

42장의 본문 질문은 요약 2회 뒤 `CONTEXT_COMPACTION_PARTIAL`로 중단됐어요. 요약이 장별 내용을 누적하며 약 4천 토큰으로 커져 4,096 출력 상한에 도달했어요. 이때까지 실제 전송은 46회, 입력 688,231·출력 45,335 토큰으로 사용자 단가 추정 **$0.6862**예요. 실패한 요약·본문 작업은 그대로 보존하고 길이 목표를 개선한 새 작업으로 아래와 같이 재검증했어요.

압축 요약의 자기강화 오류도 확인했어요. 원문 11장의 Darcy → Elizabeth 발언은 revision 3에서 맞지만 revision 4에서 반대로 바뀌고 revision 5–6이 그 오류를 유지했어요. revision 6은 공급자 완료 응답으로 저장됐지만 마지막 문장이 의미상 중간에서 끊겼어요. 길이 목표와 귀속 보존 지침의 개선은 실제 비교로 평가하며 완전한 사실성 보장으로 해석하지 않아요.

최종 회상·재조회, 도우미 실제 저장·위임·프롬프트 편집은 아래의 후속 실행으로 구분해요.

### 길이 목표만 적용한 비교와 추가 수정

최초 길이 목표를 적용한 빌드는 42장을 통과했지만 48장에서 다시 `CONTEXT_COMPACTION_PARTIAL`로 중단됐어요. 실패 2회의 출력은 각각 약 4,092 토큰이며, 해당 작업을 통과로 바꾸거나 부분 요약을 강제로 활성화하지 않았어요.

`output/memory-efficiency-ab/`에는 동일한 이전 요약·새 원문·사용자 정정을 주는 세 쌍의 실제 Gemini 비교를 보존해요. 한 쌍씩의 관찰이고 이전 출력이 다음 단계로 이어지는 누적 실험은 아니에요.

| 단계 | 이전 요약 토큰 | 최초 개선 토큰 | 관찰 |
| --- | ---: | ---: | --- |
| 새 사건과 약속 | 1,905 | 1,759 | 무작위 ID·약속은 둘 다 보존. 개선안은 Elizabeth의 반대편 비판 한 문장을 생략했어요. |
| 사용자 정정·위치 변경 | 1,174 | 1,275 | 정정의 귀속·조건부 약속·새 위치·미확인 소문을 둘 다 보존해요. |
| 잘못된 파생 요약 복구 | 1,521 | 1,482 | 직접 다시 제공한 원문으로 둘 다 발언자·대상을 복구해요. |

합계 저장 요약은 4,600→4,516 o200k 토큰으로 **1.8% 감소**지만 공급자 입력이 늘어 사용자 단가 추정 비용은 $0.02638→$0.02662였어요. 한 번씩의 지연 합계도 39.68→40.55초로 개선되지 않았어요. 이 비교는 첫 지침 변경의 비용·성능 우위를 입증하지 않으며, 기존 요약의 장별 구조를 그대로 복제하는 한계가 남았어요.

이를 근거로 요약을 현재 상태·관계·약속·정정·미해결 사건 중심으로 다시 쓰고, 지난 해결 사건은 현재에 남은 결과로 통합하도록 수정했어요. 숫자 목표도 시스템 지침에 명시해요. 원문과 이전 요약 입력을 자르지 않아요.

### 61장 완료와 최종 회상 한계

현재 상태 중심 지침을 적용한 별도 48장 작업은 요약 3회와 본문 1회로 완료했어요. 이후 54장·60장 질문을 완료하고 **61장 전부, 162,975 o200k 토큰**을 가져왔으며 본문 자동 checkpoint는 10개가 됐어요. 원래 48장 실패와 새 작업을 별도 기록해요.

48장 첫 요약의 입력 원문 hash는 이전 실패의 첫 입력과 같지만 출력은 4,045→4,055토큰으로 줄지 않았어요. 뒤따른 새 요약은 1,606·1,291토큰으로 작아졌어요. 장편을 이어갈 수 있었다는 증거이며, 같은 입력의 첫 요약 효율이나 호출 횟수 개선을 입증하지 않아요. 입력·출력과 이전 snapshot 불변 비교는 `phase-comparison.json`·`verification.json`에 보존해요.

도구 없는 최종 회상은 **보관인·검색 구절·정정 색상·최신 위치 4개를 정확히 회수**했어요. 그러나 약속·귀환 조건은 부분 실패예요. 원문에서 보관인은 Charlotte가 직접 요청할 때까지 봉인하기로 약속하고 Charlotte는 두 번째 종 뒤 문이 잠기기 전에 돌아오겠다고 말해요. 요약과 답변은 이를 암호 낭송·귀환 시각을 동시에 요구하는 반출 조건으로 합쳤어요. 단어 회수를 약속의 의미 보존으로 대체하지 않아요.

원문 검증 요청 `a88f2f55-dbff-4cda-b771-2654d9a30cd9`는 **4회 호출 뒤 `INPUT_CONTEXT_LIMIT_EXCEEDED`**로 실패했어요. 검색 결과가 쌓여 도구 응답이 문맥 사용률 91.6%와 `context.new` 권고를 알렸지만, 다음 전송 전에 모델이 문맥을 교체하지 않았어요. 실패 답변·Run·4개 attempt는 그대로 보존해요. 이후 조회 2건·짧은 원문 범위·조기 문맥 교체를 명시한 요청은 별도의 보조 지침을 제공한 평가이며 원래 실패의 통과로 세지 않아요.

이 후속 요청 `a15e642e-d1ee-4aac-9adf-403f36ded903`는 **8회 호출로 완료**했어요. `context.new`를 단독 호출하여 전송 문맥 추정을 13,577→5,004토큰으로 낮춘 뒤 검색 3회·원문 읽기 2회로 최초 사건과 1장 원문을 확인했어요. 두 원문의 범위는 각각 0–413자와 500–900자예요. 답변은 여섯 사실을 맞히고 보관인의 약속과 Charlotte의 귀환 계획을 분리하며 이행 증거가 없다고 밝혔어요. 1장의 두 반응도 실제 읽은 문장과 일치해요. 이때의 63.1% 감소는 원문 이력을 보존한 전송 문맥 교체 효과이며 전체 평가 토큰 절감률은 아니에요.

### 도우미 실제 변경과 완료 스트림 실패

`helper-lore`는 3회 호출로 채팅 전용 Archive desk 문장을 eight→nine으로 저장했어요. 도구가 제공한 원본 필드 해시와 revision으로 변경했고, 평가기에서 공유 원본의 eight 유지와 override revision 1을 확인했어요.

`helper-options`는 `options.read` 뒤 위임된 `evidenceDepth: 3`을 선택해 pending `4474e2cc-03d0-408c-9ff8-f9a1d31669c3`에 저장했어요. 그러나 세 번째 모델 호출의 완료 설명 스트림이 `UNEXPECTED_EOF`로 끊겨 **작업 자체는 실패**예요. 선택 작업 영수증·tool event·실패 답변의 영수증·현재 위임과 head가 모두 일치하는지 검증한 후, 이 옵션 선택과 모델 답변을 재실행하지 않고 독립적인 나머지 평가만 이어가요. 원 실패·3개 attempt·그전 Run 및 snapshot은 보존해요. 실제 선택의 저장/소비와 공개 답변 완료를 별도 인수 항목으로 기록해요.

프롬프트 도우미는 4회 호출로 `workspace.read`→`draft.patch`→`draft.save`를 실행하고 프리셋 revision 1→2를 저장했어요. 첫 지침에 요청한 한 문장을 추가했으며 전역 작업본은 그대로예요. 최초 평가기의 `JSON.stringify` 비교는 모델이 객체 키 순서를 바꾼 controls를 다르다고 표시했어요. 이 값은 원래 결과로 보존하고, 별도 `isDeepStrictEqual` 구조 비교로 전체 저장 모델이 요청한 문장 1회 추가와 정확히 같고 옵션·다른 블록·역할·값이 보존됐음을 확인했어요. `prompt-save-semantic-check.json`은 이 검사와 원래 비교기의 오판을 함께 기록해요.

최종 본문 예약 `3cf9ff57-d5cf-4279-8a28-9afcc2a549d8`의 snapshot에는 같은 pending ID와 `delegatedValues.evidenceDepth: 3`이 고정돼요. 로어와 옵션의 변경으로 `resources`·`promptControls` 의존성이 달라져 기존 요약을 다시 만들었어요. 새 요약 4회는 출력 919·1,486·1,295·1,400토큰으로 완료했지만 **5번째 전송이 `UNEXPECTED_EOF`로 중단**되어 최종 작업은 `CONTEXT_COMPACTION_PARTIAL`로 실패했어요. 이 실패는 출력 상한 도달이 아니며 마지막 본문 답변은 없어요. 정사·프롬프트 변경 후 요약 재구축 비용이 남아 있다는 관찰도 보존하며 의존성 검사를 완화하지 않았어요.

### 최종 사용량과 해석

장편 평가는 전송 **88회**에서 끝났어요. 완료 응답 49회·정상 도구 요청 응답 35회·부분 응답 4회이며, 부분 응답 중 2회는 사용량이 없는 EOF예요. 보고된 입력 **1,179,497**·출력 **83,891**토큰의 사용자 단가 추정은 **$1.199214**예요. 미보고 사용량은 포함하지 않았으므로 전체 비용으로 표현하지 않아요. 실제 청구액과 전체 사용량 기반 추정액은 null로 유지하고 전송 전 누적 예약 상한 **$38.4875172**를 보존해요.

61장 hash·전체 저장 원문 hash·checkpoint의 원문 참조/hash에서 불일치는 없었어요. 빌드 전환 때 보존한 snapshot 비교 112건도 불일치 0건이에요. 중복 snapshot을 다른 경계에서 다시 확인한 건수이며 고유 Run 수가 아니에요. EOF 후 독립 단계로 이어가기 전의 Run 79개·attempt 79개·도우미 작업 4개는 모든 행·snapshot hash가 같아요. 후속 원문 읽기 2곳·검색 범위 5곳·메모 읽기 2회도 원본과 일치했어요. 원본 DB·입력·실패·영수증을 보존했고, 최종 평가 상태는 `halted`예요. 따라서 장편 입력·반복 압축·후속 원문 회수·도우미의 실제 저장/예약은 관찰했지만 전체 시나리오 PASS나 문학적 품질 인수로 판정하지 않아요.
