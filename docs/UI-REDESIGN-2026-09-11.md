# 2026-09-11 UI 개편과 도우미 세션

사용자가 제공한 `우이모리 디자인 개편 시안 및 설계안`의 HTML/CSS 개별 화면 40개와 `Uimori_UI_Design_v1` 문서를 바탕으로 기존 앱을 개편해요. 이미지 시안에서는 세이지 색의 두 잎 모티프, 조용한 카드와 본문 표현을 참고해요. 브랜드는 기존 Lucide `Sprout` 아이콘으로 표현해요. 이미지에만 나타나는 통계·태그·외부 서비스 등의 기능을 추가하지 않아요.

## 화면 계약

- 탐색 폭은 248px이고 서재·프롬프트를 직접 열어요. 설정은 아래에 유지해요. 별도 포털 홈을 만들지 않아요.
- 채팅은 기본 본문 폭 760px, 서재는 최대 1280px의 왼쪽 정렬 모음, 긴 편집은 목록과 선택 항목으로 나눠요. 사용자의 읽기 설정을 유지해요.
- 프롬프트는 블록·옵션 정의·기본 옵션·협업·미리보기로 이동해요. 블록 목록은 224px이고 좁은 화면에서는 목록과 상세를 한 단계씩 열어요. JSON 대안 편집과 미적용 초안을 유지해요.
- 자료 편집은 기본 정보와 기존 아홉 분야를 같은 편집기의 열 개 섹션으로 열어요. 로어는 224px 목록과 상세로 나누고, 모바일에서는 목록과 상세를 전환해요. 섹션을 바꿔도 입력값은 유지하며 상단 저장 버튼으로 명시적으로 저장해요.
- 새 채팅은 봇·페르소나·실효 모델과 작문 프롬프트·시작 장면 순서로 확인해요. 사용 모델 설정은 본문·번역·도우미·문맥 요약을 먼저 보여주고 기타 자동 작업과 번역 오류 감지는 펼쳐서 편집해요. 연결·모델 프리셋은 별도의 탭과 저장 흐름을 유지해요.
- 도우미·창작 옵션 패널은 모두 384px이에요. 1152–1399px에서는 패널을 열 때 탐색을 임시로 접고, 탐색을 다시 펼치면 패널을 겹쳐 열어요. 더 좁은 폭에서는 겹쳐 열며 768px 미만에서는 전체 폭을 사용해요. 기존 탐색 접기 선호를 덮어쓰지 않아요.
- 본문·도우미는 공통 입력창, 요청 재시도·상태·작업 기록을 계속 사용해요. 모델 고정, 저장·적용, 미저장 초안, `false`·`0`·빈 문자열의 계약을 바꾸지 않아요.
- 탐색·아이콘 버튼은 조용한 기본 표현을 사용하고 저장·등록·보내기에 강조색을 사용해요. 도우미의 전역 모델 설정은 패널 설정에서 열어요.

## 사용자가 확정한 추가 결정

### 도우미 세션과 전개

같은 채팅·같은 전개에도 여러 도우미 세션을 만들어요. 세션 ID가 기록·개인 요약·입력 초안·원문 선택·읽는 위치·응답 cursor·재시도 키·권한의 소유자예요. 패널은 한 개이고 선택 목록에서 세션을 바꿔요. 세션을 바꾸거나 패널을 닫아도 작업을 취소하지 않아요.

세션 목록은 채팅의 모든 전개를 보여줘요. 다른 전개 소속 세션은 기록을 읽을 수 있고, 새 요청·결과 수정은 **해당 전개로 이동**한 뒤 가능해요. 이미 시작한 다른 전개의 작업은 계속 진행해요. 원문을 도우미에 보낼 때는 클릭한 원문과 그 전개의 세션 ID를 고정해요.

새 세션 생성은 요청 키로 중복을 막아요. 제목을 따로 지정하지 않았을 때 첫 요청의 앞부분을 제목으로 사용하며, 사용자가 정한 제목은 유지해요. 세션별 모델을 새로 만들지 않고 전역 도우미 모델을 사용해요. 지속 옵션 위임에는 대상 세션을 지정하며 다른 세션에 권한을 공유하지 않아요.

### 동시 실행과 삭제

전체 도우미 작업은 최대 2개까지 실행하고, 나머지는 대기해요. 각 세션에서는 한 번에 1개씩 접수 순서대로 진행해요. 취소한 작업의 공급자 요청과 worker 정리가 끝날 때까지 실행 자리를 유지해요. 앱 재시작 시 불확실한 요청을 자동 재전송하지 않아요.

세션 삭제는 채팅 삭제처럼 범위를 설명하고 확인받아요. 진행·대기 작업을 먼저 중지하고 외부 요청 종료를 기다려야 삭제할 수 있어요. 삭제 직전에는 세션 revision과 이벤트 순서를 다시 확인해요.

대화·작업·가정 장면·개인 요약·직접 권한을 삭제하고, 미적용 위임 옵션은 취소해요. 이미 본편이나 공유 자료에 저장된 결과는 유지해요. 과거 실행이 검증할 위임 영수증은 권한을 철회하고 대화 소유자를 분리하여 남겨요. 채팅·전개 자체를 삭제하면 이 영수증도 함께 정리해요. 채팅 전체 백업은 여러 세션과 이 영수증을 새 ID로 복원해요.

### 본편 지정

보관된 전개 목록에서 **기본으로 지정**을 선택해 채팅의 본편을 바꿔요. 내용을 합치거나 복제하는 기능이 아니에요. 한 채팅에는 항상 기본 전개가 하나이고, 기존 기본 전개·대상 전개의 revision을 확인하여 표시와 채팅 head를 원자적으로 갱신해요.

과거 원문·실행 snapshot·진행 중 요청·재시도는 원래 전개에 남아요. 다른 창에서 기본 전개가 바뀌어도 현재 읽는 원문과 입력의 전개를 유지해요. 채팅에 새로 진입할 때 새 기본 전개를 선택해요. 현재 기본 전개는 삭제할 수 없으며 다른 전개를 기본으로 정한 뒤 이전 전개를 삭제할 수 있어요.

## 검증

- `npm run quality:full`: PASS. 서식·lint·타입·도구 검사·빌드와 단위/통합 **1,901 PASS, opt-in 1 SKIP**예요. 마지막 실행은 2026-09-11 15:24 KST, Vitest 168.53초예요.
- `npm run verify:smoke`: F02/F03/F06 PASS. [보고서](../output/playwright/2026-09-11T06-24-21-867Z-ab0388ba/summary.json).
- `npm run verify:chat-unification`: **20 PASS**. 세션별 초안·새로고침·이름 변경·삭제·다른 전개 보호·늦게 끝나는 작업과 390/1280/1440px를 포함해요. [보고서](../output/playwright/chat-unification-2026-09-11T06-12-19-250Z-9e657807/summary.json).
- `npm run verify:navigation`: **29 PASS**. 기본 전개 교체·현재 원고/초안 유지·재진입과 마지막 작업 공간 복원을 포함해요. [보고서](../output/playwright/navigation-2026-09-11T06-19-24-072Z-206fca32/summary.json).
- `node scripts/verify-prompts.mjs`: **15 PASS**. 블록·옵션·JSON·모바일 메뉴·역할 변경의 편집 흐름을 확인했어요. [보고서](../output/playwright/prompt-editor-2026-09-11T06-24-24-768Z-9ceed9fb/summary.json).
- `npm run verify:library`: **39 PASS**. 새 채팅·자료 분야·로어·이미지·초안 충돌 검사를 기존 실행기에 함께 포함했어요. [보고서](../output/playwright/library-2026-09-11T06-20-50-940Z-49b3e777/summary.json).
- `npm run verify:providers`: **15 PASS**. [보고서](../output/playwright/provider-management-2026-09-11T06-17-37-839Z-05792adf/summary.json).
- `npm run verify:global-models`: **4 PASS**. [보고서](../output/playwright/global-models-2026-09-11T06-17-49-773Z-2e23dfc5/summary.json).

합성 브라우저는 관련 실행 6개의 총 122개 검사 결과예요. 단일 전체 회귀 실행으로 합산한 숫자가 아니에요. 각 보고서에 소스·빌드·검사 identity와 독립 DB/포트·정리 결과가 있어요. 채팅 검증 뒤에는 서재 카드 폭만, 나머지 영역 검증 뒤에는 프롬프트 상단 배치만 추가 변경했어요. 프롬프트 변경 뒤에는 해당 검사·smoke·quality:full을 다시 완료했어요. 마지막 빌드는 `31776a7af9252b3e4c08d7ae8f14fb3d5263a598a8ecbceb2d8207f736092204`예요.

초기 실패도 보존해요. 프롬프트 첫 실행은 12 PASS/3 FAIL이었고 모바일 메뉴 겹침과 검사 측 화면 전환 대기를 수정했어요. 서재 첫 실행은 24 PASS/2 FAIL로 중복 빈 목록 행동과 이전 저장 버튼 이름을 수정했고, 다음 37 PASS/2 FAIL에서 일반 자료의 편집 탐색 가정과 이미지 탭의 캡처 대상을 바로잡았어요. 위 최종 결과가 각 수정 후 실행이며 이전 FAIL을 PASS로 바꾸지 않았어요.

시각 비교와 캡처별 빌드는 [design-qa.md](../design-qa.md)의 2026-09-11 기록을 봐요. 현재 개발 데이터의 이관·구형 자료 호환은 추가하지 않았어요. 실공급자 호출·실기기·배포는 이번 로컬 검증에 포함하지 않았고, 릴리스 전 전체 `verify:redesign`도 실행하지 않았어요.

## 2026-09-11 후속: 4K·스마트폰 검토와 사용자 확정 결정

첫 개편 뒤 412×891과 2560×1440에서 다시 검토했어요. 이전 검증 범위는 390×844·1280×900·1440×900과 dark 중심이었고 두 폭은 들어 있지 않았어요. 사용자는 4K 모니터를 150% 배율로 쓰므로 CSS 뷰포트가 2560×1440이에요. 즉 이 폭이 예외 상황이 아니라 기본 작업 화면이에요.

### 확정한 결정

- **원고 폭**: 기본 880px. 읽기 설정의 `본문 폭`에서 좁게 760 / 기본 880 / 넓게 1040을 고르고 기기별로 보관해요. 집중 읽기는 별도 폭을 쓰지 않고 같은 값을 따라요.
- **입력창**: 원고 폭과 같은 축에 정렬해요. 장면 레일 여백을 한쪽에만 주던 규칙이 원고와 입력창의 중심을 29px 어긋나게 했고, 여백을 좌우 대칭으로 바꿔 해결했어요.
- **헤더**: 폭을 제한하지 않고 제목은 화면 왼쪽 끝, 도우미·모델·채팅 메뉴는 오른쪽 끝에 둬요. 화면 가장자리는 커서가 넘어가지 않아 조준이 쉽고, 읽기 폭을 바꿔도 제어 위치가 움직이지 않아요.
- **중앙정렬**: 채팅뿐 아니라 서재·프롬프트·편집기의 본문 열도 중앙정렬해요. 이는 인수 테스트 **L01(P1, 「작업 영역 좌측 24px에서 시작」)을 의도적으로 뒤집는 결정**이에요. 2560px에서 목록이 화면 왼쪽에만 몰리는 것보다 시선이 한자리에 머무는 편을 택했어요. 페이지 헤더는 전폭을 유지하므로 구분선은 화면 끝까지 이어져요.
- **탐색 접기**: 접기 버튼을 사이드바 안쪽 브랜드 옆으로 옮겼어요. 접으면 사라지지 않고 56px 아이콘 레일로 남아 펼치기·새 채팅·채팅 검색·서재·프롬프트·설정을 제공해요. 봇 트리는 접을 때 숨겨요. 1024px 미만에서는 레일 폭이 0이 되고 기존 드로어 동작을 그대로 써요.
- **패널 폭**: 도우미와 창작 옵션이 같은 우측 슬롯을 쓰므로 폭도 한 설정으로 묶어요. 일반 설정의 `도우미·창작 옵션 패널 폭`에서 좁게 360 / 보통 384 / 넓게 480을 고르고 기기별로 보관해요.
- **도킹 임계값**: 고정 breakpoint 대신 공간 예산으로 계산해요. 사이드바와 함께 도킹하려면 `248 + 패널 폭 + 768`, 레일과 함께 도킹하려면 `56 + 패널 폭 + 768`이 필요해요. 기본 384px에서는 1400과 1208이고, 480px을 고르면 1496과 1304으로 함께 올라가요.
- **창작 옵션 「이 채팅」 탭**: 설계안 P01대로 행마다의 `이 채팅에 고정` 체크박스를 `개별 지정` / `전역 따르기`로 바꿨어요. 전역을 따르는 행은 비활성 폼이 아니라 현재값을 텍스트로 보여줘요. 옵션은 프롬프트 편집기와 같은 규칙으로 그룹에 묶고 그룹마다 지정 개수를 표시해요. 옵션 45개 기준 스크롤 길이가 9,918px에서 4,526px로 줄었어요.

### 값 계약은 바꾸지 않았어요

오버라이드 여부는 그대로 **키 존재 여부**(`Object.hasOwn`)로 표현해요. `개별 지정`은 현재 유효값을 키로 넣고 `전역 따르기`는 키를 지워요. 명시적 `false` · `0` · 빈 문자열 · `null`(미설정) · 미지정의 구분, `expectedRevision` CAS, `operationId` 멱등, 위임·1회 옵션의 경로와 payload는 그대로예요.

### 그 밖에 고친 것

- 사이드바 트리 들여쓰기를 단계당 8px에서 명세값 16px로 올렸어요.
- 시각 상태를 바꾸는 `:hover` 규칙 36개를 `@media (hover: hover)`로 감쌌어요. 터치에서 탭한 자리에 hover 배경이 남아 선택되지 않은 탭이 선택된 것처럼 보이던 문제예요. `:focus-within`과 짝을 이루던 노출 규칙은 감싸지 않아 키보드 접근이 그대로예요.
- `library.css`의 `@media (max-width: 600px)` 페이지 여백 규칙이 뒤에 오는 760px 블록에 같은 특이도로 덮여 한 번도 적용되지 않고 있었어요. 중복을 지웠어요.

### 검증

- `npm run quality:full`: PASS. **1,902 PASS / 1 SKIP**, Vitest 77.25초. Node 24.14.0.
- `npm run verify:chat-prompt-options`: **5 PASS**. `npm run verify:navigation`: **29 PASS**. `npm run verify:chat-unification`: **20 PASS**. `node scripts/verify-prompts.mjs`: **15 PASS**.
- `npm run verify:library`: **38 PASS / 1 FAIL**. 실패는 `PNAV01`의 로어 본문 포커스 유지 검사예요. 같은 환경에서 손대지 않은 HEAD를 별도로 내보내 빌드해 돌려도 동일하게 실패하므로 이번 변경과 무관한 기존 실패예요. 이 Mac은 저장소 기준 환경(Windows)이 아니므로 절대 실패 수를 품질 판정에 쓰지 않아요.
- 폭별 실측(2560/1440/1300/1210/1190/1024/412/320): 가로 overflow 0건, 원고와 입력창 중심 일치, 도킹·레일·오버레이 전환이 위 예산대로 동작해요.
- 라이트 테마에서 새 옵션 행의 텍스트 대비를 실제 렌더링으로 측정했고 검사한 6종 모두 기준을 넘겨요.
- `verify:redesign` 전체와 실기기·실공급자 검증은 이번 범위에 넣지 않았어요.

### 남은 항목

- 설정 섹션 명칭(`프로바이더·모델 등록` 대 설계안의 `연결·모델 프리셋`)과 치수 토큰 전면 통일(편집기 목록 폭이 224 / 200 / 176px으로 갈려 있어요)은 결정하지 않아 손대지 않았어요.
- 일반 설정에 읽기 기본값이 없고, 기기 설정과 작업실 설정을 구분하는 범위 배지가 없어요.
- 도우미 세션 선택기는 네이티브 `select`예요. 세션이 많아지면 검색 가능한 목록 뷰로 올려야 해요.

## 2026-09-11 3차: 사용자 검토 반영

띄운 빌드를 사용자가 직접 확인한 뒤 나온 요청을 반영했어요.

- **접힌 레일의 세로 리듬**: 접으면 아이콘이 19~24px 위로 올라갔어요. 펼친 상태의 브랜드 블록이 차지하던 자리를 레일의 머리 슬롯(`--nav-head-h` 60px + 12px 여백)이 그대로 차지하게 해서, 새 채팅 87 · 검색 135 · 서재 191 · 프롬프트 239로 두 상태의 y가 완전히 같아졌어요.
- **헤더 구분선**: 본문 헤더 58px, 도우미 60px로 2px 어긋나 있었어요. 세 헤더에 `--header-h`를 적용하고 패널 제목의 행간을 본문 헤더와 맞춰 세 구분선이 모두 y=58에 놓여요.
- **도우미 진입점**: 서재·프롬프트에서 왼쪽에 있던 도우미 아이콘을 페이지 헤더 오른쪽 끝으로 옮겼어요(`headerTrailing`). 채팅 헤더와 같은 자리예요.
- **불리언 창작 옵션**: `개별 지정`을 누른 뒤 토글해야 해서 두 단계였어요. 스위치는 이미 무엇을 할지 말해 주므로, 불리언은 따름 상태에서도 스위치를 살려 두고 **한 번 넘기면 그대로 이 채팅 값이 돼요**. 되돌리기는 `전역 따르기`예요. 다른 종류는 그대로 값을 텍스트로 읽고 `개별 지정`으로 편집에 들어가요. 따름 상태에서는 옵션 설명을 접어 행 높이를 맞췄어요.
- **프로바이더 용어 통일**: UI에서 개체를 가리키던 `연결`을 `프로바이더`로 통일했어요(탭 이름, 추가 버튼, 단계 표시, 종류·이름 레이블, 저장 상태 문구). 네트워크 상태를 뜻하는 `연결됨`·`연결 확인`·`연결 해제`는 그대로예요. 접근성 이름 `프로바이더 프로토콜`은 검사 6곳이 의존해서 바꾸지 않았어요.
- **치수 토큰 통일**: `--header-h` `--nav-head-h` `--collection-w` `--detail-w` `--form-w` `--editor-list-w` `--settings-w` `--dialog-w` `--control-h`를 도입했어요. 편집기 목록 폭이 224 / 200 / 176px로 갈려 있던 것을 224px 하나로 모았고, 터치 타깃 44px 리터럴 **201개**를 `--control-h`로 바꿨어요.
- **일반 설정**: `원고 읽기` 구역을 추가해 채팅 메뉴의 읽기 설정과 같은 값을 편집해요(집중 읽기 시작 버튼은 읽는 화면에만 남겨요). 기기에만 저장되는 구역에는 `이 기기` 배지를 붙여 작업실 전체에 적용되는 설정과 구분해요.

도우미 세션 선택기는 한 채팅당 많아야 10개 정도라는 사용자 확인에 따라 네이티브 `select`를 유지해요.

### 검증

- `npm run quality:full`: PASS, **1,902 PASS / 1 SKIP**.
- `verify:chat-prompt-options` 5 PASS · `verify:navigation` 29 PASS · `verify:providers` 15 PASS · `verify:chat-unification` 20 PASS · `verify:prompts` 15 PASS.
- `verify:library` 38 PASS / 1 FAIL(`PNAV01`), `verify:package-ui` 1 FAIL(`NUI01`), `verify:settings-polish` 1 FAIL(`SCUI02`). 앞의 둘은 손대지 않은 HEAD를 따로 내보내 같은 환경에서 빌드·실행해도 똑같이 실패하는 기존 실패예요. `SCUI02`는 전체 실행에서는 통과하고 부분 묶음에서만 실패하는 것으로 이전에 측정해 둔 항목이에요.
- 도우미 진입점을 옮기면서 `tests/ui-navigation.ts`의 `openHelper`가 `.workspace-header`에서만 버튼을 찾던 것을 목적지 헤더까지 보도록 고쳤어요. 이 수정 전에는 `HELPUI03`이 실패했고, 수정 뒤 `chat-unification`이 20 PASS로 돌아왔어요.

## 2026-09-11 4차: 세부 정돈

사용자가 띄운 빌드를 훑고 준 18건을 반영했어요.

### 정렬과 간격

- **아이콘 세로정렬**: 원인이 하나로 수렴했어요. 기본 `button` 규칙에 `display`가 없어서, 자체 레이아웃을 가진 버튼을 제외한 나머지는 인라인 박스로 남고 SVG가 글자 baseline에 앉아 2~4px 내려갔어요. `button:has(> svg)`에 `inline-flex + align-items: center + gap`을 주어 아이콘을 가진 버튼만 한 번에 고쳤어요. 서재·프롬프트·설정 화면의 아이콘+텍스트 버튼 69개를 실측해 어긋난 것이 0개예요.
- **헤더·패널**: 삽화 설정의 토글 행에 `--control-h`를 주어 옆 선택 컨트롤과 높이를 맞췄어요. 오류 메시지의 닫기 버튼은 `align-items: center`로 문구와 같은 줄에 놓여요.
- **접힌 레일**: 설정 위 구분선이 펼친 상태에만 있었어요. 두 상태 모두 같은 구분선을 두고, 레일의 설정 행도 탐색 폭을 채우게 했어요.

### 모달 통일

채팅 제목 편집 모달을 기준으로 삼되, 확인 버튼을 오른쪽 끝에 두는 규칙으로 기준 자체를 맞췄어요. `.form-actions`가 이제 `justify-content: flex-end`, `gap: 10px`, 본문과 `16px` 간격을 갖고, `.danger`·`.destructive`·`.delete-control`은 `margin-right: auto`로 왼쪽 끝으로 밀려요. 조사에서 드러난 문제도 함께 고쳤어요.

- `className="danger"`에 대응 CSS가 아예 없어 파괴적 삭제 버튼이 평범한 버튼으로 그려지고 있었어요. error 색을 부여했어요.
- `DraftDiscardActions`는 `flex-direction: row-reverse`로 시각 순서를 뒤집고 있었어요. DOM 순서를 실제 순서와 맞추고 공통 행을 쓰게 바꿨어요. 버리는 동작은 왼쪽 끝, 계속 편집은 오른쪽이에요.
- `.lore-actions > button`의 `flex: 1`을 없앴어요. 이 한 곳만 버튼이 폭을 균등 분할하고 있었어요.
- 확인·취소·삭제 동작에 아이콘을 넣었어요(`DeleteButton` 한 곳이 15개 호출부를 덮어요).

### 아이콘 전용 전환

저장·뒤로가기·만들기를 글리프만 남기고 `aria-label`과 `title`로 이름을 유지했어요. 접근성 이름은 바뀌지 않아서 기존 검사가 그대로 동작해요. 빈 목록의 중앙 만들기 CTA는 텍스트를 유지해요 — 처음 쓰는 사람이 무엇을 만드는지 배우는 자리예요.

### 그 밖에

- 봇 행의 카드 테두리를 없앴어요. 트리 노드지 카드가 아니에요.
- 사이드바 폭 변화에 180ms 전환을 넣고, 펼쳐지는 하위 트리는 페이드로 나타나요. **`translateY`는 쓰지 않아요** — 드래그 정렬 중 행이 움직이면 드롭 위치가 어긋나 `ORG02`가 실패했어요. 접힌 행은 계속 언마운트 상태라 폴더 지연 로딩도 그대로예요.
- 채팅 검색 결과를 세로로 쌓았어요. 긴 봇 이름이 `flex-shrink: 0`으로 버티면서 제목을 한 글자씩 세로로 찢던 문제예요.
- 서재 폴더가 목록 보기에서 목록 행처럼 보여요(`data-view`).
- 자료 상세 `…`의 역할 전환 항목에 아이콘을 넣어 나머지 항목과 맞췄어요.
- `변경 내용·공유 영향·저장 이력`을 `변경 검토`로 줄이고 `…` 밖으로 꺼냈어요. 공유 자료 영향 범위·저장본 비교·개정 충돌·저장 이력이 들어 있어 설계안 기능 보존표의 항목이에요.
- 설정 섹션을 `프로바이더·모델`로 줄이고, UI에서 개체를 가리키던 `연결`을 모두 `프로바이더`로 통일했어요.
- 입력창 더보기의 조회 로어 제외 설명 문구를 없앴어요. 레이블이 이미 같은 말을 해요.

### 검증

`quality:full` PASS. `verify:navigation` 29 PASS · `verify:chat-unification` 20 PASS · `verify:deletion` 21 PASS · `verify:provider-management` 15 PASS · `verify:settings-polish` 15 PASS · `verify:chat-prompt-options` 5 PASS · `verify:prompts` 15 PASS. `verify:library`는 38 PASS / 1 FAIL이고 그 하나는 기존 실패 `PNAV01`이에요.

이번 변경으로 깨졌다가 고친 것: `ORG02`(애니메이션의 `translateY` 제거), `ED01·ED02`와 `DEL05`·`PMUI03`(이름이 바뀐 컨트롤을 가리키던 검사 갱신). `ORG07`은 한 번 실패했다가 재실행에서 통과해 불안정 항목으로 기록해요.

### 남은 것

`Dialog`를 쓰지 않고 직접 만든 `role="alertdialog"` 세 곳(`PackageBehaviorPanel`, `PackageImagesEditor`, `ProviderManagement`의 영향 패널)은 그대로예요. 네이티브 모달이 아니라 배경 비활성·Escape 처리가 없고, `PackageImagesEditor`는 rem·margin 기반이라 앱의 px·gap 체계와 달라요. 별도 작업으로 다뤄야 해요.
