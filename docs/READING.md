# 본문·도우미 읽기 스타일

2026-09-12 사용자 결정: 읽기 설정은 브라우저 공통으로 사용하고 작품별 설정은 필요해질 때 검토해요. 의미에 따른 줄바꿈 예외는 초기 범위에서 제외해요.

## 적용 범위

설정 → 일반 → 원고 읽기 또는 채팅 메뉴 → 읽기 설정에서 바꿔요. 읽기 스타일·간격·부호별 역할은 `uimori:readability` localStorage에 저장하며 같은 origin의 다른 탭에도 반영해요. 원문·저장된 번역·도우미의 답변·독립 가정 장면·생성 중 공개 텍스트에만 적용해요. 입력한 요청, 편집 초안, 설정 지침, 도구 JSON, 상태·진단 카드는 적용 대상이 아니에요. 설정 예문도 같은 본문 렌더러를 사용해요.

기본값은 강조·대사 줄바꿈·생각 줄바꿈 모두 꺼짐이며, 간격은 기존 화면 값을 유지해요. 기존 글꼴·본문 크기·폭 선택은 유지해요. 본문 크기는 9~28px을 1px 단위로 고르며 기본은 18px이에요. 채팅과 전역 읽기 설정이 같은 선택을 사용하고 새로고침 후에도 저장한 크기를 복원해요. 도우미의 기존 Markdown 문자와 줄바꿈도 기본값에서는 그대로 표시해요.

| 선택 | 동작 |
| --- | --- |
| 기본 | 강조·줄바꿈을 끄고 기존 간격으로 돌아가요. |
| 여유롭게 | 행간 2.2와 문단 간격 1.5em을 적용해요. |
| 대사 중심 | 은은한 강조와 대사 줄바꿈을 켜요. 생각 줄바꿈은 꺼둬요. |
| 사용자 설정 | 강조·줄바꿈·간격을 개별 변경한 상태예요. |

스타일 프리셋을 바꾸어도 직접 지정한 부호별 역할은 유지해요. ‘읽기 스타일 초기화’는 부호별 역할까지 새 읽기 스타일 설정을 초기화하며 기존 글꼴·크기·폭·기본 보기 선택은 유지해요.

## 부호와 줄바꿈

`"…"`·`“…”`·`「…」`는 대사, `'…'`·`‘…’`는 생각, `『…』`는 일반 인용이 기본이에요. 각 부호를 대사·생각·일반 인용·처리 안 함으로 바꿀 수 있어요. 실제 문맥의 의미를 추론하거나 새 모델 호출을 하지 않아요.

완결된 최상위 인용의 앞뒤를 화면에서만 나눠요. 기존 줄바꿈과 문단 경계는 보존하고 줄바꿈이 없는 방향에만 표시용 경계를 추가해요. 중첩 인용의 내부에서는 다시 줄을 나누지 않고, 짝이 닫히지 않은 인용은 그대로 표시해요. 기본적인 영문 apostrophe와 코드·URL·태그 속성·ruby·보호 토큰은 인용 분석에서 제외해요. 본문 Markdown의 굵게·기울임에 걸친 인용도 스타일을 유지해요. 도우미와 생성 중 텍스트는 기존처럼 Markdown 자체를 문자로 표시하면서 인용 스타일을 적용해요.

`“괜찮아”라고 그녀는 말했다.`도 대사 줄바꿈이 켜져 있으면 닫는 부호 뒤에서 분리돼요. 조사·서술 결합·작품명 등 문맥별 예외는 사용자 실사용 후 후속 검토 대상으로 남겨요. 긴 문단이나 원문 블록을 넘어 부호를 억지로 이어 맞추지 않아요. 생성 중에는 닫는 부호가 도착한 완결 범위부터 강조·분리가 나타날 수 있어요.

## 원문 보존과 구현

`web/reading-preferences.ts`가 기본값과 브라우저 저장값 검증을, `web/useReadingPreferences.ts`가 저장·탭 반영·표시 간격·읽던 블록/메시지 위치 복원을 맡아요. 설정은 `web/ReadabilitySettings.tsx`, 본문과 도우미 표시는 `web/Prose.tsx`, 부호 짝맞춤은 `web/reading-quotes.ts`, 스타일은 `web/reading.css`예요.

표시용 React 요소와 CSS만 바꿔요. 저장된 원문·번역문·hash·offset·block anchor와 이미지 귀속은 바꾸지 않아요. 기존 패키지 표시 변환 뒤의 화면 텍스트에 적용하고, 모델 입력·사용량·백업·내보내기에는 읽기 설정을 넣지 않아요. 본문·가정 장면 복사 버튼은 기존 저장 텍스트를 복사해요. 드래그 선택 복사는 브라우저의 시각적 개행 처리를 따를 수 있어요.

검사는 `tests/prose.test.ts`, `tests/reading-quotes.test.ts`, `tests/reading-preferences.test.ts`와 `tests/reading-browser.spec.ts`에 있어요. 관련 화면 검사는 `npm run verify:ui`에 연결해요. 합성 브라우저 증거와 실제 휴대폰·개인 작품에서의 읽기 효과는 구분해요.

## 2026-09-12 검증

- `release:check -- --area verify:ui`: 품질 검사 **1,988 PASS / 1 opt-in SKIP**, 최소 앱 smoke **3 PASS**, 관련 UI 브라우저 **26 PASS**와 기존 본문 단위 **9 PASS**예요.
- 별도 읽기 화면 **2 PASS**: 412/2560px에서 원문·번역·도우미 적용 범위, 저장 텍스트·복사·anchor 보존, 기존 개행 높이 유지, 읽던 위치, 여러 탭의 설정, 9/28px 적용·새로고침 복원을 확인했어요. 밝은/어두운 본문과 두 폭의 설정·도우미 캡처도 검토했어요.
- 초기 검사에서 발견한 라벨 연결과 테스트의 오래된 화면 이름·팝업 폭 기대값은 보완 후 위 검사로 재확인했어요. 실패한 원 실행 기록은 그대로 보존해요.
- 사용자 요청에 따라 전체 `verify:redesign` 재실행은 생략했어요. 공통 폭을 참조하는 다른 기능 묶음 전체와 실기기·개인 작품의 읽기 효과까지 검증한 결과는 아니에요.
