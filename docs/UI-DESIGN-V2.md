# UI 설계 v2 · 원고가 화면이다

2026-09-08 실제 실행 화면(데스크톱 1440×900, 모바일 390×844 합성 뷰포트)을 검토한 뒤 작성한 **제안 설계**예요. 지금까지의 [UI 원칙](UI-PRINCIPLES.md)·[화면 설계](UI-SCREEN-DESIGN.md)·개별 계약(응답별 작업 현황, 장면 네비게이터, 입력창 구성, 포크)이 각각은 합리적이었지만, 원고 앞뒤에 한 줄씩 쌓여 채팅 화면이 무거워졌어요. 이 문서는 그 결정들을 **한 화면 단위로 다시 판단**하고, 필요한 곳은 뒤집어요. 구현·검증 전이며, 아래 시안은 합성 자료로 만든 정적 HTML이에요.

시안: [`ui-redesign/v2/uimori-ui-v2-prototype.html`](../ui-redesign/v2/uimori-ui-v2-prototype.html) · [모바일 채팅](../ui-redesign/v2/mobile-chat.png) · [모바일 상태·메뉴](../ui-redesign/v2/mobile-chat-states.png) · [데스크톱 채팅](../ui-redesign/v2/desktop-chat.png) · [데스크톱 서재](../ui-redesign/v2/desktop-library.png) · [모바일 서재](../ui-redesign/v2/mobile-library.png) · [모바일 자료 상세](../ui-redesign/v2/mobile-detail.png)

## 한 문장 목표

**채팅을 열면 원고가 보이고, 손이 가는 자리에 입력창이 있고, 나머지는 만졌을 때만 나타나요.** ChatGPT·Codex가 간결하게 느껴지는 이유는 기능이 적어서가 아니라, 상시 표시 요소가 메시지·입력창·헤더 세 가지뿐이고 그 밖의 모든 것이 대상 가까이에서 호출될 때만 나타나기 때문이에요. Uimori는 원고 읽기 앱이라 이 원칙이 더 강하게 적용돼야 해요.

## 세 층으로 나눠요

모든 화면 요소를 다음 세 층 중 하나에 배정하고, 새 요소를 추가할 때도 층부터 정해요.

| 층 | 뜻 | 채팅 화면의 예 |
| --- | --- | --- |
| **0 · 상시** | 항상 보여요. 한 화면에 최소한만 둬요. | 헤더 한 줄, 원고 본문, 요청 말풍선, 입력창 한 줄, 진행·실패 카드(해당 턴 안에서만) |
| **1 · 근접** | 항상 존재하지만 연하게 그리고, 마우스를 올리거나 장면을 누르면 진해져요. 터치에서는 연한 상태로 보여요. | 장면 헤더 줄(번호·상태·보기 전환), 장면 끝 액션 줄, 스크롤 중 "최신으로" 버튼 |
| **2 · 열어서** | 메뉴·시트·대화상자로 열어요. 이름과 아이콘을 함께 보여요. | 장면 ⋯, 채팅 ⋯, 입력창 ⋯, 작업 상세, 채팅 설정, 읽기 설정 |

원칙 문서의 "필요한 상태는 보여요"는 유지해요. 다만 **완료는 상태가 아니에요.** 진행·실패·불확실만 0층에 두고, 완료는 1층의 작은 체크 표시로 남겨요.

## 수치 목표

원칙 문서의 시작값을 이어받고, 채팅 화면에 없던 목표를 추가해요. 합성 검사에서 고정할 수 있는 값이에요.

| 항목 | 목표 | 현재 실측(2026-09-08) |
| --- | --- | --- |
| 모바일 채팅 첫 화면에서 본문이 차지하는 높이 | **60% 이상** (844px 중 506px 이상) | 약 33% |
| 요청 말풍선 아래 ~ 첫 문장 | **40px 이하** (장면 헤더 28 + 간격) | 약 200px |
| 빈 입력창 높이 | 모바일 **52px**, 데스크톱 **56px** (칩·상태 줄 제외) | 162 / 173px |
| 장면 끝 ~ 다음 요청 사이의 조작부 | **32px 한 줄** | 약 250px (세 줄 + 여백) |
| 헤더 | 56px 한 줄, 제어 4개 이하 | 56px, 데스크톱 5개 |
| 터치 영역 | 모든 제어 44×44 이상 (1층 아이콘은 32px 그림 + 44px 영역) | 드로어 메뉴 36×40 |

## 채팅 화면

### 헤더

`[탐색] [제목 / 봇 · 페르소나로] … [모델 칩] [읽기 T] [집중 ⛶] [⋯]`. 모바일은 `[☰] [제목/부제] [모델 칩] [⋯]` 네 개예요.

- **부제에 페르소나를 합쳐요.** "등대지기 미라 · 서연으로". 리더 첫 줄의 봇 칩은 없애요.
- **모델은 헤더의 칩이에요.** 현재 본문 모델 이름을 항상 보여주고, 누르면 본문 모델 선택 메뉴가 열려요. 입력창에서 모델을 빼는 대신 여기서 "본문 모델 확인" 요구를 채워요. 모바일은 짧은 이름(프리셋 이름의 마지막 단어 또는 12자)만 보여요.
- **채팅 ⋯ 메뉴**: 채팅 설정, 여기까지 새 채팅으로 복사(포크), 이름 변경, 작업 현황, 보관된 전개, 읽기 설정(모바일), 집중 읽기(모바일), 채팅 삭제. 포크는 이름이 있는 항목으로만 제공하고 헤더의 단독 아이콘은 없애요. 확인 단계 없이 즉시 복사하는 결정은 유지하되, 복사 뒤 "원본으로 돌아가기" 링크가 있는 짧은 알림을 보여줘요.

### 턴의 구조

요청 말풍선 → 장면 헤더 한 줄 → 본문 → 장면 끝 액션 줄. 이 넷만이에요.

- **요청 말풍선**은 오른쪽 정렬과 배경색으로 화자를 드러내요. "내 요청" 라벨은 없애요.
- **장면 헤더 줄(28px, 1층)**: 왼쪽 `장면 3` + 상태 표시 + `수정됨` 배지, 오른쪽 `번역 | 원문` 작은 전환. 상태 표시는 완료 ✓(연함), 진행 ⟳, 실패 ⚠ + 짧은 문구, 불확실 ?예요. 누르면 **작업 상세** 시트가 열려요. 이 시트가 응답별 작업 현황 계약(요청별 독립, 기본 접힘, 펼침 선택 보존, 펼치기만으로 호출 없음)을 그대로 이어받아요. 번역이 없는 장면은 전환 대신 **`번역 보기`** 텍스트 버튼 하나를 보여줘요. 이 버튼은 모델을 호출하므로 이름을 유지해요.
- **본문**은 읽기 설정의 서체·크기·줄간격을 그대로 따라요. 최대 폭 760px.
- **장면 끝 액션 줄(32px, 1층)**: `복사 · 번역/원문 전환 · 수정 · ⋯`. 데스크톱은 장면에 마우스를 올렸을 때 진해지고, 터치에서는 연한 상태로 항상 보여요. **⋯ 메뉴**: 현재 설정으로 다시 번역, 여기서 새 채팅으로 복사, 이미지 선택, 표시 문구 바꾸기, 작업 상세와 원문 연결 정보. "표시 문구 바꾸기"와 "원문 연결 정보"의 상시 접기 항목은 없애요.
- **진행 중 턴**: 장면 헤더 아래에 `⟳ 장면을 쓰는 중 · 12초 [중지]` 한 줄과 흐린 자리표시 세 줄. 입력창의 보내기 버튼은 중지 버튼으로 바뀌어요.
- **실패 턴**: 본문 자리에 카드 하나. 제목(무엇이 실패했는지), 한 줄 설명, `다시 시도 · 모델 설정 · 상세`. 실패한 원고를 원문으로 만들지 않고 요청은 그대로 남겨요. 입력창 위의 고정 실패 줄은 **화면에 없는 턴**의 실패나 보조 작업(다른 장면의 번역)에만 사용하고, 사용자가 해당 턴을 보면 사라져요. 경과 시간은 1초 이상일 때만 표시해요.

### 입력창

두 단계로 모양이 바뀌어요. **비어 있거나 한 줄일 때**는 `[⋯] [텍스트] [↑]` 한 줄이에요. **두 줄 이상으로 늘어나면** 텍스트가 위에서 전체 폭을 쓰고, 그 아래 한 줄에 `[⋯]`가 왼쪽, `[↑]`가 오른쪽에 놓여요. 버튼이 본문 옆에 떠 있지 않게 하려는 규칙이며, 전환은 줄 수만으로 결정해요. 보조 선택(창작 프리셋, 페르소나, 조회 로어 제외)이 있을 때만 입력창 위에 칩 한 줄을 보여주고, 칩의 ×로 해제해요. ⋯ 버튼 자체는 선택 여부로 강조하지 않아요. 칩이 그 역할을 해요. 모델명 캡션 줄과 단축키 캡션 줄은 없애요. 단축키는 placeholder 끝의 흐린 글자(데스크톱)와 보내기 버튼의 툴팁, 설정 → 일반에서 확인해요.

### 장면 탐색

데스크톱은 왼쪽의 얇은 눈금 레일을 유지해요. **모바일은 헤더 아래의 고정 바를 없애고** 두 가지로 대체해요. 헤더 제목을 누르면 장면 목록 시트(번호·요청 검색·페이지)가 열리고, 스크롤이 마지막 장면을 벗어나면 입력창 위 오른쪽에 `↓` 둥근 버튼이 나타나 "최신 장면으로" 이동해요. 다운로드 화살표 아이콘은 쓰지 않아요.

### 빈 채팅

봇 아바타(48px)와 "첫 장면을 들려주세요." 한 줄, 그 아래 한 줄 안내를 화면 세로 중앙에 둬요. 패키지에 시작문이 있으면 시작 선택지를 칩으로 최대 3개 보여줘요.

### 집중 읽기

탐색·입력창을 숨기는 현재 정의에 **1층 요소도 숨김**을 더해요. 장면 헤더 줄과 액션 줄은 본문을 눌렀을 때만 잠시 나타나요. 데스크톱 눈금 레일은 유지해요.

## 탐색

### 데스크톱 사이드바(260px)

위에서 아래로 `[Uimori · 접기] [새 채팅] [채팅 검색] [봇 전환 행] [채팅 목록] … [진행 중 N] [서재] [프롬프트] [설정]`이에요.

- **봇 전환 행** `[아바타] 등대지기 미라 ▾` 하나가 "← 봇 목록", 봇 카드, "채팅 4개"를 대신해요. 누르면 봇 목록 팝오버(검색, 최근 순, "서재에서 관리")가 열려요. 개수는 표시하지 않아요.
- **채팅 목록**은 미분류 채팅을 헤더 없이 위에 평평하게, 이름이 있는 폴더만 접이식 헤더로 보여요. "채팅 폴더" 라벨과 폴더 만들기 아이콘은 없애고, 폴더 만들기는 봇 전환 행의 메뉴와 채팅 ⋯의 "폴더로 이동 → 새 폴더"에서 해요. 행은 34px, 마우스를 올리면 ⋯가 나타나요. 드래그 정렬·폴더 드롭·hover 삭제는 현재 계약을 유지해요.
- **작업 현황은 탐색 항목이 아니에요.** 진행 중 작업이 있을 때만 하단에 `⟳ 진행 중 N` 한 줄이 나타나고, 누르면 작업 현황이 열려요. 없으면 줄 자체가 없어요.
- **서재 화면에서도 사이드바는 그대로 채팅 목록이에요.** 사이드바에 봇 목록을 다시 그리지 않아요. 이것이 서재 본문과의 중복을 없애는 유일한 방법이에요.

### 모바일 드로어

같은 구성의 전체 높이 시트예요. "탐색" 제목은 없애고 오른쪽 위 닫기만 둬요. 채팅 ⋯는 항상 보이며 44×44예요.

## 서재와 프롬프트

- **헤더** `[서재] … [+ 만들기]`, 그 아래 분절 탭 `봇 · 페르소나 · 모듈`, 그 아래 **도구 한 줄** `[폴더 ▾] [검색] [목록 조정]`. 폴더 드롭다운 마지막 항목이 "폴더 관리…"예요. 데스크톱의 별도 폴더 패널은 없애서 두 번째 PanelLeft 아이콘 문제와 목록 폭 문제를 함께 해결해요. 목록 조정 아이콘은 SlidersHorizontal이에요(정렬·카드/목록·선택).
- **행** 64px: 아바타 40, 이름 한 줄(모바일은 두 줄), 설명 한 줄. 오른쪽 `새 채팅 · ⋯`는 데스크톱에서 마우스를 올렸을 때 진해지고 터치에서는 연하게 항상 보여요. 페르소나·모듈 행은 ⋯만 있어요.
- **자료 상세**: 헤더 `[←] [이름 / 분류 · 폴더] [편집] [⋯]`. 본문은 아바타 72 + 이름 + 소개, **주 행동 `새 채팅` 하나**, 보조 `최근 채팅 이어가기 · 제목`, 개수 한 줄(`로어 3 · 지침 2 · 이미지 1`, 모두 0이면 생략), 본문. ⋯에 페르소나로 사용, 모듈로 추가, 복제, 분류·폴더 이동, 삭제를 둬요. 네 개의 역할 카드와 역할 설명 문장은 없애요. 역할 설명은 페르소나·모듈 탭의 빈 화면과 선택기에 남아요.
- **편집**: 헤더 `[←] [이름 또는 새 봇] [⋯(삭제)]`, 하단 `저장 · 취소`(새 자료는 `등록`). 저장 뒤 같은 자리에 `채팅 시작`을 보여주는 현재 동작은 유지하되 저장이 주 행동이에요. 홀로 떠 있는 삭제 아이콘은 ⋯로 들어가요.
- **프롬프트 목록**은 서재와 같은 도구 한 줄 `[폴더 ▾] [검색] [목록 조정(역할 필터·정렬·선택)]`을 써요. 폴더 칩 줄과 "전체 · 3개" 개수 줄은 없애요.

## 설정

- **채팅 설정**은 여섯 분야를 두 묶음으로 보여요. **기본**: 인물과 자료, 프롬프트와 프리셋, 모델. **고급**: 상태와 기억, 이미지, 자동 후속 작업. 묶음 이름은 목록의 작은 구분 라벨이에요. 읽기 설정은 채팅 설정에서 빼요.
- **읽기 설정**(헤더 T 또는 채팅 ⋯)은 기본 보기, 본문 글꼴, 본문 크기, 집중 읽기 시작만 다뤄요. 화면 테마는 **설정 → 일반**에서만 바꿔요. 같은 값을 세 곳에서 바꾸지 않아요.
- **모델 이름 표기**를 한 가지로 통일해요. 어디서든 `프리셋 이름`이 첫째 줄이고 `연결 이름`이 흐린 둘째 줄이에요. 모델 ID는 모델 편집 화면에서만 보여요. select 안에서도 같은 순서를 지켜 잘림이 생기지 않게 해요.
- 그 밖의 설정·연결·데이터 관리 화면은 현재 구현을 유지해요.

## 아이콘 어휘 v2

[화면 설계](UI-SCREEN-DESIGN.md)의 아이콘 표를 이어받고 아래를 바꾸거나 더해요. 규칙은 하나예요. **Ellipsis는 "이 대상의 메뉴"에만 쓰고, 목록 전체를 조정하는 도구는 SlidersHorizontal이에요.**

| 의미 | v2 아이콘 | 비고 |
| --- | --- | --- |
| 포크(여기까지 새 채팅으로 복사) | GitFork | 메뉴 항목으로만, 이름과 함께. Copy는 "복사(클립보드)"에만 |
| 최신 장면으로 | ArrowDown (둥근 플로팅 버튼) | ArrowDownToLine은 쓰지 않아요 |
| 목록 조정(정렬·보기·선택·필터) | SlidersHorizontal | 서재·프롬프트·모델 목록 공통 |
| 폴더 관리 | FolderCog | 폴더 드롭다운 안의 항목 |
| 사이드바 접기 | PanelLeft | 이 의미에만 사용 |
| 번역 ↔ 원문 전환 | Languages | 장면 끝 액션 줄 |
| 장면 상태 | Check · Loader · TriangleAlert · CircleHelp | 색만으로 뜻을 전하지 않고 실패·불확실에는 짧은 문구를 붙여요 |
| 삭제 | Trash2 | 모든 메뉴에서 아이콘과 함께 |
| 새 채팅(행 안) | MessageSquarePlus | 유지 |

## 뒤집는 결정

아래는 기록된 결정과 다르게 가는 항목이에요. 구현 시 각 문서를 함께 고쳐요.

| 기존 결정 | 출처 | v2 |
| --- | --- | --- |
| 요청과 본문 사이에 접이식 작업 현황 패널을 항상 표시 | [TURN-ACTIVITY](TURN-ACTIVITY.md) | 장면 헤더의 상태 표시 + 작업 상세 시트. 응답별 독립·기본 접힘·펼침 보존·호출 없음 계약은 시트가 이어받아요 |
| 모바일 헤더 아래 장면 탐색 바 상시 표시 | [SCENE-NAVIGATION](../project-plan/SCENE-NAVIGATION.md) | 제목 탭 → 장면 목록 시트, 스크롤 시 플로팅 최신 버튼. 데스크톱 레일은 유지 |
| 기본 입력창에 본문 모델 select 포함 | [UIUX-IMPROVEMENTS C06](../project-plan/UIUX-IMPROVEMENTS-2026-09-08.md) | 모델은 헤더 칩. 입력창은 한 줄 |
| 헤더의 포크 아이콘 버튼 | [UI-RESULTS 포크 간략화](../project-plan/UI-RESULTS.md) | 채팅 ⋯와 장면 ⋯의 이름 있는 항목. 즉시 복사 동작은 유지 |
| "현재 설정으로 새 번역" 버튼을 본문 위에 상시 표시 | 현행 구현 | 장면 ⋯의 "현재 설정으로 다시 번역" |
| 데스크톱 서재의 폴더 패널 | [UI-SCREEN-DESIGN](UI-SCREEN-DESIGN.md) | 모든 폭에서 폴더 드롭다운 |
| 자료 상세의 역할 카드 4개 | [LIBRARY-RESULTS](../project-plan/LIBRARY-RESULTS.md) | 주 행동 하나 + ⋯ |
| 채팅 설정 7분야(읽기 설정 포함), 테마 3곳 | [UI-DETAIL-IMPLEMENTATION](../project-plan/UI-DETAIL-IMPLEMENTATION.md) | 6분야 두 묶음, 테마는 설정 → 일반 |
| 사이드바의 봇 카드·봇 목록·개수 | [REDESIGN](../project-plan/REDESIGN.md) 화면 흐름 | 봇 전환 행 하나. 서재에서도 채팅 목록 유지 |
| 입력창 캡션 줄(모델명·단축키) | 현행 구현 | 없음 |

## 바꾸지 않는 계약

원문 귀속과 hash, 번역·재번역의 명시적 호출, CAS와 초안 보호, 삭제 확인 절차와 참조 보호, 진행·실패·불확실 상태의 발견 가능성과 취소 접근, 봇 소속 고정과 폴더 상속, 포크의 무호출 복사, 읽기 설정 존중, 접근 가능한 이름과 44px 터치 영역, 색만으로 의미를 전하지 않는 규칙은 그대로예요. 화면을 얇게 만들기 위해 이 중 어느 것도 약화하지 않아요.

## 적용 순서와 검증

한 번에 갈아엎지 않고 네 묶음으로 나눠요. 각 묶음은 `npm run quality`, 빌드, 아래 `verify:*`, 6개 폭(360·390·430·768·1024·1440)의 화면 확인으로 닫아요. 관련 문서는 같은 변경에서 고쳐요.

1. **채팅 리더**: 장면 헤더 줄과 작업 상세 시트, 장면 끝 액션 줄과 ⋯, 진행·실패 카드, 봇 칩 제거, 집중 읽기 확장. 검증 `verify:turn-activity`, `verify:ui`, `verify:loading`. 문서 TURN-ACTIVITY, USAGE. 완료 기준: 모바일 첫 화면 본문 60% 이상, 요청~첫 문장 40px 이하.
2. **헤더·입력창·장면 탐색**: 모델 칩, 채팅 ⋯, 한 줄 입력창과 칩, 플로팅 최신 버튼, 모바일 장면 목록 시트. 검증 `verify:ui`, `verify:loading`, `verify:redesign`. 문서 SCENE-NAVIGATION, UIUX-IMPROVEMENTS C06 주석, USAGE.
3. **탐색과 서재**: 봇 전환 행, 평평한 미분류, 진행 중 줄, 폴더 드롭다운, 도구 한 줄(서재·프롬프트), 상세·편집 헤더. 검증 `verify:navigation`, `verify:library`. 문서 LIBRARY, UI-SCREEN-DESIGN, DELETION(삭제 위치 문구).
4. **설정 정리**: 채팅 설정 두 묶음, 읽기 설정 축소, 테마 단일 위치, 모델 표기 통일. 검증 `verify:redesign`, `verify:providers`. 문서 USAGE, UI-DETAIL-IMPLEMENTATION 주석.

각 묶음 뒤 실기기(S25U) 확인은 별도 기록이에요. 합성 뷰포트 통과를 실기기 통과로 적지 않아요.

## 이 설계의 한계

- 시안은 정적 HTML이고 hover·시트·키보드 동작은 그려져 있지 않아요. 구현에서 초점 순서와 Escape 처리를 기존 대화상자·메뉴 규칙에 맞춰요.
- 장면 헤더의 상태 표시는 색과 모양만으로 뜻을 전하지 않도록 실패·불확실에 문구를 붙였지만, 스크린리더 이름은 구현에서 확인해야 해요.
- 모바일 장면 목록 시트와 플로팅 최신 버튼은 스크롤 위치 복원 계약(URL·읽기 위치)을 바꾸지 않는 범위에서만 유효해요.
- 이 문서는 채팅·탐색·서재·설정의 배치를 다루고, 프롬프트 편집기 내부(기본 지침 우선 편집, C14)와 채팅 이름 변경·재생성(C05·C12)은 다음 설계 범위로 남겨요.
